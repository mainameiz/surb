== Surb

This gem contains useful set of rake tasks, rails generators, scripts and so on for easy application development with RubyOnRails

== Usage

Simply write this line in your Gemfile

  gem 'surb'

and run:

  bundle install
